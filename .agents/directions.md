# Directions

Open work only. Shipped behavior belongs in `CHANGELOG.md`.

- Add a documented runtime-refresh command that updates scaffold-owned files
  without overwriting unit-owned `slides.js` or assets.
- Add browser-level smoke coverage for navigation, overview, fullscreen, and a
  local interactive iframe.
- Decide the long-term compatibility promise before declaring `1.0.0`.
